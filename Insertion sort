#include<stdio.h>
#include<math.h>

int n,i,j,x,a[10001];

int main(){
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(i=1;i<n;i++){
        j=i;
        x=a[i];
        while(j>0 && a[j-1]>x){
            a[j]=a[j-1];
            j--;
        }
        a[j]=x;
    }
    for(i=0;i<n;i++){
        printf("%d\n",a[i]);
    }
}
