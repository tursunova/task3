#include<stdio.h>
#include<math.h>

int n,i,j,x,a[10001],b[10001];
int* merge_sort(int *a,int *b, int l,int r){
    int i;
    if (l==r){
        b[l]=a[l];
        return b;
    }
    int m=(l+r)/2;
    int *l_b=merge_sort(a,b,l,m);
    int *r_b=merge_sort(a,b,m+1,r);
    int *t=l_b==a ? b:a;
    int w=r-l,l_cur=l,r_cur=m+1;
    for(i=l;i<=r;i++){
        if (l_cur<=m && r_cur <=r){
            if (l_b[l_cur]<r_b[r_cur]){
                t[i]=l_b[l_cur];
                l_cur++;
            } else {
                t[i]=r_b[r_cur];
                r_cur++;
            }
        } else if (l_cur<=m){
            t[i]=l_b[l_cur];
            l_cur++;
        } else {
            t[i]=r_b[r_cur];
            r_cur++;
        }
    }
    return t;
}

int main(){
    int b[1001];
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    int m[10001];
    *m=*merge_sort(a,b,0,n-1);
    for(i=0;i<n;i++){
        printf("%d\n",a[i]);
    }
}
